function sum(x, y) {
    let ergebnis = x + y;
    return ergebnis;
}

sum(10, 2);
console.log("Ergebnis = " + sum(2, 3));

function div(a, b) {
    let division = a / b;
    return division;
}

console.log( "Division: " + div(15, 3));
/*
AUFGABE:
1) Funktion anlegen für Subtraktion oder Multiplikation oder Division
2) Aufruf dieser Funktion
*/